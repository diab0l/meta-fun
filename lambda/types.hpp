#ifndef __LAMBDA_TYPES_HPP__
#define __LAMBDA_TYPES_HPP__

namespace lambda {
    template<typename>
    struct var {};

    template<typename, typename>
    struct abs {};

    template<typename, typename>
    struct app {};
}

#endif 